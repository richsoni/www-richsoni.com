import { storiesOf } from "@storybook/react";
import * as React from 'react';

import {{ pascalCase componentName }} from '.';

storiesOf("{{ pascalCase componentName }}", module).add('Default', () => <{{ pascalCase componentName }} />);
